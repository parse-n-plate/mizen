# Cursor AI Rules for Mizen

## 🛠️ Tech Stack Overview

This project uses a modern, maintainable tech stack designed for both developer and designer friendliness:

- **TypeScript** — for strictly typed, safer JavaScript development  
- **React (functional components & hooks)** — for building dynamic UIs using best practices  
- **Next.js** — for efficient server-side rendering and project structure  
- **Solar Icons** — please use the Solar Icons library for all iconography to ensure a consistent, accessible visual language. **Default to the filled version** of icons unless specifically requested otherwise ([Solar Icons Documentation](https://icons.solar/))  
- **CSS Modules / Tailwind CSS** — utility-first styling (see [globals.css](src/app/globals.css))  
- **Error Boundaries** — for robust async and error handling in UI

## User Context & Communication
- Audience: Product designer, limited coding experience, prefers detailed explanations
- Communication: Always include educational, step-by-step explanations using clear visual signals (⚠️ 🛑 ⏸️ 📋 ✅)

## Coding & Review Guidelines
- Add clear, detailed comments—explain both what you're doing and why
- For large/risky changes: signal with ⚠️ or 🛑, explain, and await user confirmation before proceeding
- Use explicit TypeScript types, avoid `any`, and define interfaces for complex data
- Use functional React components, hooks, and error boundaries for async ops

## UI Component Rules
- **Globals CSS:** Always review and update `src/app/globals.css` if you change or add any `/components/ui/` component
    - Add CSS variables for colors, typography, spacing if needed
    - Add shared utility/component classes if required
    - Organize CSS logically (by layer: base, components, utilities)
- When in doubt, update globals.css for design tokens and shared styles
- **Dropdowns/Popovers:** Always use `modal={false}` to prevent scroll locking and layout shift

## Documentation & Workflow
- Update docs (`README.md`, etc.) for new features, breaking changes, or complex logic
- Before large changes: signal, explain, get confirmation
- After changes: explain updates, suggest testing, remind to update changelogs

## Development Commands
- Common scripts:
    - `npm run dev` — Start dev server
    - `npm run build` — Build for production
    - `npm run lint` — Check/fix code
    - `npm run docker:*` — Docker build/run

